{% extends 'base.html.twig' %}

{% block title %}Acceuil
{% endblock %}

{% block body %}
	<h1 class="text-center m-5">Les beaux jours arrivent !
	</h1>
	<div class="container">
		<div class="row">
			{% for p in products %}
				<div class="col">
					<div class="card">
						<img src="images/{{p.mainpicture}}" alt="image du produit" class="img-fluid">
						<div class="card-body">
							<h4 class="card-little">{{ p.name }}
								({{ p.price / 100}}
								EUR )</h4>
							<span class="badge badge-info text-bg-dark mb-2 mt-2">{{ p.category.name }}</span>
							<p class="card-text">{{ p.shortDescription }}</p>
							<a href="#" class="btn btn-secondary btn-sm">Détails</a>
							<a href="#" class="btn btn-dark btn-sm">Ajouter</a>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>

{% endblock %}
